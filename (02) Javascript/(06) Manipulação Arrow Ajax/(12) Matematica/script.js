let novoValor = Math.floor(Math.random() * 100)
   // round() = Arredonda pra baixo se dor ate 4, e arredonda pra cima apartir de 5
   // floor() = Arredonda pra baixo
   // ceil() = Arredonda pra cima
   // abs() = Numero absoluto
   // min() = Retorna o menor numero entre os valores
   // max() = Retorna o maior numero entre os valores
   // random() = Gera um numero aleatorio entre 0 - 1
console.log(novoValor)