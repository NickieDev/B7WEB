let lista = ['ovo', 'macarrao', 'feijao', 'pipoca']

console.log( Object.keys(lista) )
console.log( Object.values(lista) )
console.log( Object.entries(lista) ) // Coloca cada item dentro de um array com seu index

let pessoa = {
   nome: 'Edawrd',
   sobrenome: 'Newgate',
   idade: 90
}

console.log( Object.keys(pessoa) )
console.log( Object.values(pessoa) )
console.log( Object.entries(pessoa) ) // oloca cada item dentro de um array com sua chave e valor